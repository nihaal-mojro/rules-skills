# This repo IS the workspace's `.cursor` folder. Ignore anything Cursor or a
# developer creates locally in it that should not be shared.

# Personal MCP config (can contain tokens)
mcp.json

# Cursor-generated / personal working files
plans/
*.local.*

# OS / editor noise
.DS_Store
Thumbs.db
